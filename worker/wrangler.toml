name = "tg-cloud-drive-worker"
main = "src/index.ts"
compatibility_date = "2025-03-01"
compatibility_flags = ["nodejs_compat"]

# === Environment variables (set via wrangler secret or dashboard) ===
# TG_BOT_TOKEN             — Telegram Bot Token from @BotFather
# STORAGE_CHANNEL_ID       — Channel ID where bot stores files (bot must be admin, format: -100...)
# DRIVE_AUTH_TOKEN         — Bearer token for web app authentication

# === D1 — Metadata database ===
[[d1_databases]]
binding = "DB"
database_name = "tgcd-meta"
database_id = ""

# === KV — Share links (keep existing pattern) ===
[[kv_namespaces]]
binding = "SHARES"
id = ""
preview_id = ""
